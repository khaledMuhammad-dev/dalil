# The root package is a thin orchestrator: it owns no app dependencies and must
# not try to hoist or link the two app repos (each keeps its own lockfile and
# node_modules, installed via `pnpm install:all`).
ignore-workspace-root-check=true
